package main

import "fmt"

func main() {
	user_login_try := 10

	// Because 10 is NOT greater than 10.
	// Because 10 is NOT less than 10.
	// if user_login_try < 10

	if user_login_try > 10 {
		fmt.Println("Pls slow down ! Try again after few minutes")

	} else {

		fmt.Println("Login complete ")
	}

	// else if {}
	// no switch ?
	//
	//
}
